version: v0
kind: Pipeline
name: default
steps:
  - name: install-dependency
    image: node:latest
    commands:
      - npm install
    next: build-code
  - name: build-code
    image: node:latest
    commands:
      - npm run build
