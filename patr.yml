version: v0
kind: Pipeline
name: default
steps:
  - name: Install Dependency
    image: node:latest
    commands:
      - npm install
    next:
      - Build Code
  - name: Build Code
    image: node:latest
    commands:
      - npm run build
